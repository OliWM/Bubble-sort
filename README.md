Bubble sorts an array of either numbers or letters.

With headerfile and docs (doxygen)


## Compile and run
To compile quick and dirty with clang just

```bash
#for macOs/Linux
clang -std=gnu2y -g -o main src/main.c
```
or
```bash
#for Windows (I think this is how)
clang -std=gnu2y -g -o main.exe src/main.c
```

And then run
```bash
#for macOs/Linux
./main
```
Or
```cmd
#for Windows (I think this is how)
main.exe
```

## Flowchart

![Flowchart of simplified bubble sort](https://i.imgur.com/yeQa8Mi.png)

